# Reverse proxy with automatic HTTPS. WebSockets are proxied transparently.
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy game:3000
	header {
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
	}
}
